"use client";

import { ForgeHeroScene } from "@/components/three/hero/ForgeHeroScene";

/** Scene entry — delegates to modular hero composition */
export function ForgeScene() {
  return <ForgeHeroScene />;
}
